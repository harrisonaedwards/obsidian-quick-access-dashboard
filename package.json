{
  "name": "obsidian-quick-access-dashboard",
  "version": "0.1.1",
  "description": "Files and folders you add, recent files, and most-opened files in one local Obsidian sidebar.",
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/harrisonaedwards/obsidian-quick-access-dashboard.git"
  },
  "type": "commonjs",
  "scripts": {
    "test": "node build.js && node --test test/*.test.js",
    "build": "node build.js",
    "check": "node build.js && node --check model.js && node --check plugin.js && node --check main.js && node --test test/*.test.js"
  }
}
